#include<stdio.h>
   int main(){
   int a, b, sum, mult, divi;
   printf("enter any number");
   scanf("%d %d",&a,&b);

   sum = (a + b);
   mult= (a * b); 
   divi = (a / b);

   printf("sum is %d\n",sum);
   printf("mul is %d\n",mult);
   printf("div is %d\n",divi);

   return 0;
 }
